define(["repo/show_repo"], function (show_repo) {
  // console.log("repo loading");

  function init() {}
  init();

  // console.log("repo loaded");

  return {
    init: init,
    show_repo: show_repo
  };
});
